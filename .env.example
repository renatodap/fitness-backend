# ==============================================================================
# Wagner Coach Backend - Environment Configuration
# ==============================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control!
# ==============================================================================

# ------------------------------------------------------------------------------
# Application Settings
# ------------------------------------------------------------------------------
ENVIRONMENT=development                    # Options: development, staging, production, test
DEBUG=true                                 # Enable debug mode (disable in production!)
LOG_LEVEL=INFO                            # Options: DEBUG, INFO, WARNING, ERROR, CRITICAL

# ------------------------------------------------------------------------------
# API Configuration
# ------------------------------------------------------------------------------
# CORS_ORIGINS: Comma-separated list of allowed origins
CORS_ORIGINS=http://localhost:3000,https://wagnercoach.com
# WARNING: Set ALLOW_ALL_ORIGINS=false in production!
ALLOW_ALL_ORIGINS=false

# ------------------------------------------------------------------------------
# Database (Supabase)
# ------------------------------------------------------------------------------
# Get these from: https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-anon-key-here
SUPABASE_SERVICE_KEY=your-service-role-key-here

# ------------------------------------------------------------------------------
# AI API Keys
# ------------------------------------------------------------------------------
# OpenAI (for embeddings, GPT models)
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# Anthropic Claude (for coach chat, program generation)
# Get from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-...

# Groq (for fast, cheap inference - quick entry text)
# Get from: https://console.groq.com/keys
GROQ_API_KEY=gsk_...

# OpenRouter (for image processing with Llama Vision)
# Get from: https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-...

# ------------------------------------------------------------------------------
# Redis (for caching and background jobs)
# ------------------------------------------------------------------------------
REDIS_URL=redis://localhost:6379

# ------------------------------------------------------------------------------
# Celery (background task processing)
# ------------------------------------------------------------------------------
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# ------------------------------------------------------------------------------
# Authentication & Security
# ------------------------------------------------------------------------------
# JWT_SECRET: Use a strong random string (min 32 characters)
# Generate with: openssl rand -hex 32
JWT_SECRET=your-super-secret-jwt-key-min-32-chars
JWT_ALGORITHM=HS256

# CRON_SECRET: For securing cron endpoints
# Generate with: openssl rand -hex 16
CRON_SECRET=your-cron-secret-key

# WEBHOOK_SECRET: For securing webhook endpoints (Strava, Garmin)
# Generate with: openssl rand -hex 16
WEBHOOK_SECRET=your-webhook-secret-key

# ------------------------------------------------------------------------------
# Monitoring & Error Tracking
# ------------------------------------------------------------------------------
# Sentry DSN (optional, for error tracking in production)
# Get from: https://sentry.io/settings/YOUR_ORG/projects/YOUR_PROJECT/keys/
SENTRY_DSN=

# ==============================================================================
# Production Deployment Checklist
# ==============================================================================
# Before deploying to production, ensure:
# [ ] ENVIRONMENT=production
# [ ] DEBUG=false
# [ ] ALLOW_ALL_ORIGINS=false
# [ ] CORS_ORIGINS set to actual frontend domain(s)
# [ ] All API keys are production keys (not test/dev keys)
# [ ] JWT_SECRET is strong and unique (min 32 chars)
# [ ] SENTRY_DSN configured for error monitoring
# [ ] All secrets are stored securely (Railway secrets, not in code)
# ==============================================================================